Note that 474Project.ipynb will only work with the following files
from the yelp-dataset. This dataset cannot be pushed to github since it's
file size will exceed the limit. Therefore you have to have a separate
directory and then overwrite 474Project if you want to push

* review.json
* business.json
* user.json
